{
    "name": "@coveops/result-badge",
    "version": "1.2.2",
    "description": "",
    "main": "dist/index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "build": "coveops build ResultBadge --template typescript",
        "watch": "coveops build ResultBadge --template typescript --watch",
        "serve": "coveops serve",
        "precompile": "npm run build"
    },
    "author": "",
    "license": "Apache-2.0",
    "dependencies": {
        "@coveops/localization-manager": "^1.0.1",
        "@coveops/turbo-core": "^1.2.1",
        "coveo-search-ui": "^2.10085.2"
    },
    "types": "dist/index.d.ts",
    "files": [
        "dist"
    ],
    "devDependencies": {
        "@coveops/cli": "^0.16.0"
    }
}
